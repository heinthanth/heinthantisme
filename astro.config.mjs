import{defineConfig}from"astro/config";import svelte from"@astrojs/svelte";import sveltePreprocess from"svelte-preprocess";import sitemap from"@astrojs/sitemap";import siteURL from"./src/misc/siteURL.mjs";export default defineConfig({site:siteURL,build:{format:"file"},integrations:[svelte({preprocess:sveltePreprocess()}),sitemap()],vite:{css:{modules:{generateScopedName:"[hash:base64:12]"}}}});
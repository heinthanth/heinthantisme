import{defineConfig}from"astro/config";import svelte from"@astrojs/svelte";import sveltePreprocess from"svelte-preprocess";import sitemap from"@astrojs/sitemap";import siteURL from"./src/misc/siteURL.mjs";import turboLinks from"@astrojs/turbolinks";import robotsTxt from"astro-robots-txt";import{createHash}from"crypto";export default defineConfig({site:siteURL,build:{format:"file"},integrations:[svelte({preprocess:sveltePreprocess()}),sitemap(),turboLinks(),robotsTxt()],experimental:{integrations:!0},vite:{css:{modules:{generateScopedName:(e,t,s)=>{const r=s.indexOf(`.${e}`),o=s.substring(0,r).split(/[\r\n]/).length;return createHash("sha1").update(`.${e}@${t}${o}=>${s}`).digest("base64").slice(0,11).replace(/[\+\/\=]/g,"_").replace(/^(\d|-)/,"_$1").slice(0,10)}}}}});